## Updating

1. Update the firmware version in .github.workflows/build.yml as well as config/west.yml
2. Git push
3. Wait for the firmware to build and download & unpack it
4. Connect a side with USB, double press the white button
5. Copy firmware onto the newly connected CORNE device
